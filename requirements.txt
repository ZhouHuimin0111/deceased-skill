# 逝者.skill (Deceased.Skill) 依赖环境

# 核心数据解析依赖
Pillow>=9.0.0          # 用于读取老照片的 EXIF 元数据与时间线

# 辅助解析增强（推荐安装，用于处理早期复杂的聊天记录格式）
chardet>=5.0.0         # 自动检测并修复历史文本文档的乱码问题
python-dateutil>=2.8   # 更精准的跨平台时间戳解析